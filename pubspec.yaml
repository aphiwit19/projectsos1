name: projectappsos
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.12.1
  firebase_auth: ^5.5.1
  cloud_firestore: ^5.6.5
  geolocator: ^13.0.2
  url_launcher: ^6.3.0  # อัพเดตเป็นเวอร์ชันล่าสุด
  permission_handler: ^11.3.1
  sensors_plus: ^5.0.1
  intl: ^0.18.0
  shimmer: ^3.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true